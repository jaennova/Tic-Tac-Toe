<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TicTacToe" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">
        <!-- Colores de la marca -->
        <item name="colorPrimary">@color/line</item>
        <item name="colorPrimaryVariant">@color/line</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/line</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/background</item>

        <!-- Colores de fondo -->
        <item name="android:colorBackground">@color/background</item>

        <!-- Personaliza tu tema aquí -->
    </style>

    <!-- Estilo para las líneas del tablero -->
    <style name="BoardLines" parent="Theme.TicTacToe">
        <item name="android:background">@color/black</item>
    </style>
</resources>
